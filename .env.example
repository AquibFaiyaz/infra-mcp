# Remote VPS settings
VPS_HOST=85.257.131.125
VPS_USER=root
# Optional SSH options, e.g. -i /path/to/key -p 2222
VPS_SSH_OPTIONS=

# Local commands
DISK_USAGE_COMMAND=df -h
MEMORY_USAGE_COMMAND=free -m

# VPS commands
VPS_DISK_USAGE_COMMAND=df -h
VPS_MEMORY_USAGE_COMMAND=free -m
VPS_DOCKER_COMMAND=docker ps

# Postgres container settings
POSTGRES_CONTAINER=postgres-db
POSTGRES_USER=username
POSTGRES_DB=test_db
POSTGRES_SCHEMA=public
VPS_PSQL_COMMAND=psql
